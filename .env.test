# Test environment configuration
MODE=test
LOG_LEVEL=DEBUG
RUN_INTERVAL_SECONDS=60
TRADING_INTERVAL_MINUTES=15
MAX_TRADES_PER_DAY=10

# AI Configuration
DEFAULT_AI_PROVIDER=requesty
DEFAULT_AI_MODEL=parasail/parasail-gemma3-27b-it
OPENAI_MODEL_NAME=gpt-4

# Demo mode settings
DEMO_MODE=true

# Test API keys (replace with actual keys in production)
OPENAI_API_KEY=test_key
REQUESTY_API_KEY=test_key
DEEPSEEK_API_KEY=test_key
OPENROUTER_API_KEY=test_key

# Test Robinhood credentials (replace with actual credentials in production)
ROBINHOOD_USERNAME=test_user
ROBINHOOD_PASSWORD=test_pass
ROBINHOOD_MFA_SECRET=test_secret
