[pytest]
testpaths = tests
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
markers =
    integration: mark a test as an integration test
    timeout: mark a test with a timeout
    flaky: mark a test as flaky (may need reruns)
python_files = test_*.py
